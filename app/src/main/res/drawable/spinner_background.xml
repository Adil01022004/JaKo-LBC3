<shape xmlns:android="http://schemas.android.com/apk/res/android">
    <corners android:radius="20dp" /> <!-- Здесь задается радиус углов в dp -->
    <solid android:color="@android:color/white" /> <!-- Цвет фона, можете настроить под свой дизайн -->
    <stroke
        android:width="1dp"
        android:color="@android:color/holo_green_dark" /> <!-- Обводка, можете настроить ширину и цвет -->
</shape>
